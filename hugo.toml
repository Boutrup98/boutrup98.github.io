baseURL = 'https://jbld.co'
languageCode = 'en-us'
locale = 'en-us'
title = 'jbl design'
description = 'Personal portfolio and blog of designer, Jacob Boutrup Lorensen'

# Don't publish original bundle images — only the optimized WebP variants we reference.
[[cascade]]
  [cascade.build]
    publishResources = false

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[params]
  author = "Jacob Boutrup Lorensen"
  email = "Boutrup98@gmail.com"
  description = "Personal portfolio and blog showcasing design work, projects, and thoughts on design philosophy"
  
  enableSearch = true
  enableComments = false
  
[menu]
  [[menu.main]]
    name = "About"
    url = "/about/"
    weight = 1
  [[menu.main]]
    name = "Work"
    url = "/work/"
    weight = 2
  [[menu.main]]
    name = "Posts"
    url = "/posts/"
    weight = 3
  [[menu.main]]
    name = "Projects"
    url = "/projects/"
    weight = 4

[outputs]
  home = ["HTML", "RSS"]

# Disable taxonomies (no tags/categories)
[taxonomies]

[assets]
  [assets.css]
    minify = true
  [assets.js]
    minify = true

[security]
  [security.exec]
    allow = ['^dart-sass-embedded$', '^go$', '^npx$', '^postcss$', '^babel$']
    osEnv = ['(?i)^(PATH|PATHEXT|APPDATA|TMP|TEMP|TERM)$']
    [security.exec.env]
      HUGO_ENVIRONMENT = 'production'
      HUGO_ENV = 'production'
